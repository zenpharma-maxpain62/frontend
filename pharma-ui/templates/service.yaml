apiVersion: v1
kind: Service
metadata:
  name: {{ include "pharma-ui.fullname" . }}
  labels:
    {{- include "pharma-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pharma-ui.selectorLabels" . | nindent 4 }}
